# Supabase Configuration (Production)
# Get these values from: https://app.supabase.com/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Development Database (Optional)
# Use a separate Supabase project for development to safely test schema changes
# If not set, the app will use the production database above
# When running in Vite dev mode (npm run dev), the app automatically uses dev database if these are configured
VITE_SUPABASE_DEV_URL=your_dev_supabase_url_here
VITE_SUPABASE_DEV_ANON_KEY=your_dev_supabase_anon_key_here

# Supabase Project References (for CLI commands)
# Get these from: Supabase Dashboard → Project Settings → General → Reference ID
# Dev project ref is used by db:push:dev, db:reset:dev, db:diff:dev
# Prod project ref is used by db:push:prod
SUPABASE_DEV_PROJECT_REF=your_dev_project_ref_here
SUPABASE_PROD_PROJECT_REF=your_prod_project_ref_here

# TMDB API (Movies & TV Shows)
# Get your key from: https://www.themoviedb.org/settings/api
VITE_TMDB_API_KEY=your_tmdb_api_key_here

# RAWG API (Games)
# Get your key from: https://rawg.io/apidocs
VITE_RAWG_API_KEY=your_rawg_api_key_here

# Google Books API
# Get your key from: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_BOOKS_API_KEY=your_google_books_api_key_here

# NOTE: Music uses iTunes Search API which requires NO API key! 🎉
# Just works out of the box - search songs, albums, artists for free

# Admin User ID
# This is YOUR Supabase user ID - only you can access the admin panel
# Get it from: Supabase Dashboard → Authentication → Users → Copy your ID
# NOTE: Super-admin protection is handled by database triggers, not environment variables
VITE_ADMIN_USER_ID=your_user_id_here

# NOTE: All variables must start with VITE_ to be accessible in the browser
# Copy this file to .env.local and fill in your actual values
